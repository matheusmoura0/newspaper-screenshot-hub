diff --git a/app/assets/stylesheets/application.css b/app/assets/stylesheets/application.css
index fe93333..1413e51 100644
--- a/app/assets/stylesheets/application.css
+++ b/app/assets/stylesheets/application.css
@@ -1,10 +1,20 @@
-/*
- * This is a manifest file that'll be compiled into application.css.
- *
- * With Propshaft, assets are served efficiently without preprocessing steps. You can still include
- * application-wide styles in this file, but keep in mind that CSS precedence will follow the standard
- * cascading order, meaning styles declared later in the document or manifest will override earlier ones,
- * depending on specificity.
- *
- * Consider organizing styles into separate files for maintainability.
- */
+:root { --ink:#16211f; --muted:#68736f; --line:#dfe6e2; --paper:#f5f7f4; --accent:#b6282f; --accent-dark:#891f25; --success:#24714c; --danger:#ad3434; }
+* { box-sizing:border-box; }
+body { margin:0; background:var(--paper); color:var(--ink); font-family:Inter,ui-sans-serif,system-ui,-apple-system,sans-serif; }
+a { color:inherit; text-decoration:none; }
+.app-header { height:76px; padding:0 clamp(20px,5vw,72px); background:#fff; border-bottom:1px solid var(--line); display:flex; align-items:center; justify-content:space-between; position:sticky; top:0; z-index:10; }
+.brand { display:flex; gap:10px; align-items:center; }
+.brand-mark { width:40px; height:40px; display:grid; place-items:center; background:var(--accent); color:#fff; font-weight:900; letter-spacing:-1px; }
+.brand strong,.brand small { display:block; line-height:1.05; }.brand small{color:var(--muted);font-size:11px;letter-spacing:.08em;text-transform:uppercase;margin-top:4px}
+.app-header nav { display:flex; align-items:center; gap:24px; font-size:14px; font-weight:650; }.app-header nav form{margin:0}.link-button,.text-action{border:0;background:none;color:var(--accent);font:inherit;cursor:pointer;padding:0}
+.page-shell { width:min(1220px,calc(100% - 40px)); margin:0 auto; padding:44px 0 80px; }.auth-shell{min-height:100vh;display:grid;place-items:center;padding:24px}
+.page-heading,.section-heading { display:flex; align-items:flex-end; justify-content:space-between; gap:24px; margin-bottom:28px; }.page-heading h1{font-family:Georgia,serif;font-size:clamp(32px,4vw,52px);letter-spacing:-.035em;margin:2px 0 8px}.section-heading h2{font-family:Georgia,serif;font-size:30px;margin:0}
+.eyebrow{color:var(--accent);font-size:11px;font-weight:800;letter-spacing:.14em;margin:0 0 8px}.muted{color:var(--muted);margin:0;line-height:1.6}
+.button{display:inline-flex;justify-content:center;align-items:center;border:1px solid transparent;border-radius:6px;padding:11px 17px;font-weight:750;cursor:pointer}.button.primary{background:var(--accent);color:#fff}.button.primary:hover{background:var(--accent-dark)}.button.secondary{border-color:var(--line);background:#fff}
+.stats-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:16px}.stat{background:#fff;border:1px solid var(--line);border-top:3px solid var(--ink);padding:22px}.stat span{color:var(--muted);font-size:13px;display:block}.stat strong{font-family:Georgia,serif;font-size:38px;display:block;margin-top:10px}.stat.success{border-top-color:var(--success)}.stat.danger{border-top-color:var(--danger)}
+.section-block{margin-top:48px}.gallery-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:18px}.capture-card{background:#fff;border:1px solid var(--line);border-radius:8px;overflow:hidden;transition:.2s}.capture-card:hover{transform:translateY(-3px);box-shadow:0 14px 32px #17231f12}.capture-image{aspect-ratio:4/5;background:#e9eeeb;overflow:hidden}.capture-image img{width:100%;height:100%;object-fit:cover;object-position:top}.capture-meta{padding:15px}.capture-meta strong,.capture-meta span{display:block}.capture-meta span{font-size:12px;color:var(--muted);margin-top:5px}
+.panel,.table-card,.filter-bar{background:#fff;border:1px solid var(--line);border-radius:8px}.panel{padding:28px}.table-card{overflow:auto}table{width:100%;border-collapse:collapse}th,td{text-align:left;padding:16px 18px;border-bottom:1px solid var(--line);font-size:14px}th{font-size:11px;letter-spacing:.08em;text-transform:uppercase;color:var(--muted)}td small{display:block;color:var(--muted);margin-top:4px;max-width:420px;overflow:hidden;text-overflow:ellipsis}.status{padding:5px 9px;border-radius:99px;font-size:11px;font-weight:800}.status.ok{background:#e4f3ea;color:var(--success)}.status.off{background:#ecefed;color:var(--muted)}
+.stack-form{display:grid;gap:10px}.stack-form label,legend{font-size:13px;font-weight:750}.stack-form input,.stack-form select,.filter-bar input,.filter-bar select{width:100%;border:1px solid #cdd6d1;border-radius:6px;background:#fff;padding:11px 12px;font:inherit}.form-grid{display:grid;grid-template-columns:1fr 1fr;gap:18px}.form-grid>div{display:grid;gap:8px}.form-grid .full{grid-column:1/-1}fieldset{border:1px solid var(--line);border-radius:6px;padding:16px;margin:10px 0}.check{display:inline-flex!important;align-items:center;gap:8px;margin-right:22px}.check input{width:auto}.form-actions{display:flex;justify-content:flex-end;gap:10px;margin-top:10px}.form-errors,.flash.alert{background:#fce8e8;color:#8a2424;border:1px solid #efc2c2;padding:13px;border-radius:6px}.flash.notice{background:#e8f5ed;color:#236443;border:1px solid #bfdfcb;padding:13px;border-radius:6px}.flash{margin-bottom:20px}.narrow-panel{max-width:620px}.detail-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:18px}.detail-grid span,.detail-grid strong{display:block}.detail-grid span{font-size:12px;color:var(--muted);margin-bottom:6px}
+.auth-card{width:min(440px,100%);background:#fff;border:1px solid var(--line);padding:38px;border-radius:10px;box-shadow:0 22px 60px #17231f14}.auth-brand{margin-bottom:38px}.auth-card h1{font-family:Georgia,serif;font-size:38px;margin:0 0 10px}.auth-card .stack-form{margin:28px 0 18px}.auth-link{color:var(--accent);font-size:13px;font-weight:700}.filter-bar{padding:14px;display:grid;grid-template-columns:180px 1fr 180px auto;gap:10px;margin-bottom:24px}.empty-state{grid-column:1/-1;border:1px dashed #bec9c3;padding:50px;text-align:center;color:var(--muted)}.empty-state strong{color:var(--ink)}.screenshot-view{background:#dfe5e1;padding:24px;border-radius:8px;text-align:center}.screenshot-view img{max-width:100%;height:auto;box-shadow:0 12px 35px #0002}.inline-form{display:inline}.text-action{font-size:12px}
+@media(max-width:900px){.stats-grid,.gallery-grid{grid-template-columns:repeat(2,1fr)}.detail-grid{grid-template-columns:1fr 1fr}.app-header nav a{display:none}.filter-bar{grid-template-columns:1fr 1fr}.filter-bar .button{grid-column:1/-1}}
+@media(max-width:600px){.app-header{height:66px;padding:0 18px}.page-shell{width:min(100% - 28px,1220px);padding-top:28px}.page-heading{align-items:flex-start;flex-direction:column}.page-heading .button{width:100%}.stats-grid{grid-template-columns:1fr 1fr}.stat{padding:16px}.stat strong{font-size:30px}.gallery-grid{grid-template-columns:1fr}.form-grid,.detail-grid,.filter-bar{grid-template-columns:1fr}.auth-card{padding:26px 22px}.form-actions{flex-direction:column-reverse}.form-actions .button{width:100%}}
diff --git a/app/controllers/admin/base_controller.rb b/app/controllers/admin/base_controller.rb
new file mode 100644
index 0000000..5cdc3e4
--- /dev/null
+++ b/app/controllers/admin/base_controller.rb
@@ -0,0 +1,12 @@
+class Admin::BaseController < ApplicationController
+  before_action :require_admin
+
+  private
+    def require_admin
+      redirect_to root_path, alert: "Você não tem permissão para acessar esta área." unless current_user&.admin?
+    end
+
+    def record_activity(action, auditable = nil, metadata = {})
+      current_user.activity_logs.create!(action:, auditable:, metadata:)
+    end
+end
diff --git a/app/controllers/admin/newspapers_controller.rb b/app/controllers/admin/newspapers_controller.rb
new file mode 100644
index 0000000..b76566d
--- /dev/null
+++ b/app/controllers/admin/newspapers_controller.rb
@@ -0,0 +1,58 @@
+class Admin::NewspapersController < Admin::BaseController
+  before_action :set_newspaper, only: %i[show edit update destroy]
+
+  def index
+    @newspapers = Newspaper.alphabetical
+  end
+
+  def show
+  end
+
+  def new
+    @newspaper = Newspaper.new(defaults)
+  end
+
+  def create
+    @newspaper = Newspaper.new(newspaper_params)
+    if @newspaper.save
+      record_activity("newspaper.created", @newspaper)
+      redirect_to admin_newspaper_path(@newspaper), notice: "Jornal cadastrado com sucesso."
+    else
+      render :new, status: :unprocessable_entity
+    end
+  end
+
+  def edit
+  end
+
+  def update
+    if @newspaper.update(newspaper_params)
+      record_activity("newspaper.updated", @newspaper)
+      redirect_to admin_newspaper_path(@newspaper), notice: "Jornal atualizado com sucesso."
+    else
+      render :edit, status: :unprocessable_entity
+    end
+  end
+
+  def destroy
+    if @newspaper.destroy
+      record_activity("newspaper.deleted", nil, { name: @newspaper.name })
+      redirect_to admin_newspapers_path, notice: "Jornal removido.", status: :see_other
+    else
+      redirect_to admin_newspaper_path(@newspaper), alert: "O jornal possui capturas e não pode ser removido."
+    end
+  end
+
+  private
+    def set_newspaper
+      @newspaper = Newspaper.find(params[:id])
+    end
+
+    def newspaper_params
+      params.require(:newspaper).permit(:name, :slug, :homepage_url, :category, :country, :time_zone, :capture_time, :desktop_enabled, :mobile_enabled, :active)
+    end
+
+    def defaults
+      { country: "Brasil", time_zone: "America/Sao_Paulo", capture_time: "08:00", desktop_enabled: true, mobile_enabled: true, active: true }
+    end
+end
diff --git a/app/controllers/admin/users_controller.rb b/app/controllers/admin/users_controller.rb
new file mode 100644
index 0000000..29aa848
--- /dev/null
+++ b/app/controllers/admin/users_controller.rb
@@ -0,0 +1,42 @@
+class Admin::UsersController < Admin::BaseController
+  before_action :set_user, only: %i[update]
+
+  def index
+    @users = User.order(:name)
+  end
+
+  def new
+    @user = User.new(role: :member, active: true)
+  end
+
+  def create
+    @user = User.new(user_params.merge(password: SecureRandom.base58(32), invited_at: Time.current))
+    if @user.save
+      PasswordsMailer.reset(@user).deliver_later
+      record_activity("user.invited", @user)
+      redirect_to admin_users_path, notice: "Convite enviado para #{@user.email_address}."
+    else
+      render :new, status: :unprocessable_entity
+    end
+  end
+
+  def update
+    if @user == current_user && params.dig(:user, :active) == "0"
+      redirect_to admin_users_path, alert: "Você não pode desativar a própria conta."
+    elsif @user.update(user_params.slice(:role, :active))
+      record_activity("user.updated", @user)
+      redirect_to admin_users_path, notice: "Usuário atualizado."
+    else
+      redirect_to admin_users_path, alert: @user.errors.full_messages.to_sentence
+    end
+  end
+
+  private
+    def set_user
+      @user = User.find(params[:id])
+    end
+
+    def user_params
+      params.require(:user).permit(:name, :email_address, :role, :active)
+    end
+end
diff --git a/app/controllers/application_controller.rb b/app/controllers/application_controller.rb
index 5f38f02..1773f25 100644
--- a/app/controllers/application_controller.rb
+++ b/app/controllers/application_controller.rb
@@ -1,8 +1,14 @@
 class ApplicationController < ActionController::Base
   include Authentication
+  helper_method :current_user
   # Only allow modern browsers supporting webp images, web push, badges, import maps, CSS nesting, and CSS :has.
   allow_browser versions: :modern
 
   # Changes to the importmap will invalidate the etag for HTML responses
   stale_when_importmap_changes
+
+  private
+    def current_user
+      Current.user
+    end
 end
diff --git a/app/controllers/dashboard_controller.rb b/app/controllers/dashboard_controller.rb
index 391fa2e..9ce28b5 100644
--- a/app/controllers/dashboard_controller.rb
+++ b/app/controllers/dashboard_controller.rb
@@ -1,4 +1,8 @@
 class DashboardController < ApplicationController
   def index
+    @capture_run = CaptureRun.recent.first
+    @recent_screenshots = Screenshot.completed.recent.includes(:newspaper, image_attachment: :blob).limit(8)
+    @active_newspapers = Newspaper.active.count
+    @users_count = User.active.count if current_user.admin?
   end
 end
diff --git a/app/controllers/passwords_controller.rb b/app/controllers/passwords_controller.rb
index f95ec78..18c9b5f 100644
--- a/app/controllers/passwords_controller.rb
+++ b/app/controllers/passwords_controller.rb
@@ -19,10 +19,11 @@ class PasswordsController < ApplicationController
 
   def update
     if @user.update(params.permit(:password, :password_confirmation))
+      @user.update_column(:accepted_invitation_at, Time.current) if @user.invited_at? && @user.accepted_invitation_at.nil?
       @user.sessions.destroy_all
-      redirect_to new_session_path, notice: "Password has been reset."
+      redirect_to new_session_path, notice: "Senha definida com sucesso."
     else
-      redirect_to edit_password_path(params[:token]), alert: "Passwords did not match."
+      redirect_to edit_password_path(params[:token]), alert: "As senhas não coincidem."
     end
   end
 
diff --git a/app/controllers/screenshots_controller.rb b/app/controllers/screenshots_controller.rb
new file mode 100644
index 0000000..5803ccd
--- /dev/null
+++ b/app/controllers/screenshots_controller.rb
@@ -0,0 +1,15 @@
+class ScreenshotsController < ApplicationController
+  def index
+    @date = params[:date].present? ? Date.iso8601(params[:date]) : (Screenshot.maximum(:captured_on) || Date.current)
+    @screenshots = Screenshot.completed.where(captured_on: @date).includes(:newspaper, image_attachment: :blob)
+    @screenshots = @screenshots.where(newspaper_id: params[:newspaper_id]) if params[:newspaper_id].present?
+    @screenshots = @screenshots.where(viewport: params[:viewport]) if params[:viewport].present?
+    @newspapers = Newspaper.alphabetical
+  rescue Date::Error
+    redirect_to screenshots_path, alert: "Data inválida."
+  end
+
+  def show
+    @screenshot = Screenshot.completed.includes(:newspaper, image_attachment: :blob).find(params[:id])
+  end
+end
diff --git a/app/controllers/sessions_controller.rb b/app/controllers/sessions_controller.rb
index cf7fccd..8d74c95 100644
--- a/app/controllers/sessions_controller.rb
+++ b/app/controllers/sessions_controller.rb
@@ -6,16 +6,17 @@ class SessionsController < ApplicationController
   end
 
   def create
-    if user = User.authenticate_by(params.permit(:email_address, :password))
+    if (user = User.authenticate_by(params.permit(:email_address, :password))) && user.active?
       start_new_session_for user
+      user.update_column(:last_sign_in_at, Time.current)
       redirect_to after_authentication_url
     else
-      redirect_to new_session_path, alert: "Try another email address or password."
+      redirect_to new_session_path, alert: "E-mail ou senha inválidos."
     end
   end
 
   def destroy
     terminate_session
-    redirect_to new_session_path, status: :see_other
+    redirect_to new_session_path, notice: "Sessão encerrada.", status: :see_other
   end
 end
diff --git a/app/mailers/passwords_mailer.rb b/app/mailers/passwords_mailer.rb
index 4f0ac7f..bb5a258 100644
--- a/app/mailers/passwords_mailer.rb
+++ b/app/mailers/passwords_mailer.rb
@@ -1,6 +1,6 @@
 class PasswordsMailer < ApplicationMailer
   def reset(user)
     @user = user
-    mail subject: "Reset your password", to: user.email_address
+    mail subject: "Defina sua senha — Newspaper Screenshot Hub", to: user.email_address
   end
 end
diff --git a/app/models/activity_log.rb b/app/models/activity_log.rb
index f260a10..d1a63a0 100644
--- a/app/models/activity_log.rb
+++ b/app/models/activity_log.rb
@@ -1,3 +1,8 @@
 class ActivityLog < ApplicationRecord
   belongs_to :user
+  belongs_to :auditable, polymorphic: true, optional: true
+
+  validates :action, presence: true
+
+  scope :recent, -> { order(created_at: :desc) }
 end
diff --git a/app/models/capture_run.rb b/app/models/capture_run.rb
index deb84da..5d816ee 100644
--- a/app/models/capture_run.rb
+++ b/app/models/capture_run.rb
@@ -1,2 +1,9 @@
 class CaptureRun < ApplicationRecord
+  has_many :screenshots, dependent: :destroy
+
+  enum :status, { pending: 0, running: 1, completed: 2, completed_with_errors: 3, failed: 4 }, default: :pending
+
+  validates :scheduled_for, presence: true, uniqueness: true
+
+  scope :recent, -> { order(scheduled_for: :desc) }
 end
diff --git a/app/models/newspaper.rb b/app/models/newspaper.rb
index 9a8fb0e..4296c09 100644
--- a/app/models/newspaper.rb
+++ b/app/models/newspaper.rb
@@ -1,2 +1,22 @@
 class Newspaper < ApplicationRecord
+  has_many :screenshots, dependent: :restrict_with_error
+
+  before_validation :set_slug
+
+  validates :name, :slug, :homepage_url, :time_zone, :capture_time, presence: true
+  validates :slug, uniqueness: true
+  validates :homepage_url, format: URI::DEFAULT_PARSER.make_regexp(%w[http https])
+  validate :at_least_one_viewport
+
+  scope :active, -> { where(active: true) }
+  scope :alphabetical, -> { order(:name) }
+
+  private
+    def set_slug
+      self.slug = name.to_s.parameterize if slug.blank?
+    end
+
+    def at_least_one_viewport
+      errors.add(:base, "selecione desktop ou mobile") unless desktop_enabled? || mobile_enabled?
+    end
 end
diff --git a/app/models/screenshot.rb b/app/models/screenshot.rb
index dc1b8c9..37c0215 100644
--- a/app/models/screenshot.rb
+++ b/app/models/screenshot.rb
@@ -1,4 +1,14 @@
 class Screenshot < ApplicationRecord
   belongs_to :newspaper
   belongs_to :capture_run
+
+  has_one_attached :image
+
+  enum :viewport, { desktop: 0, mobile: 1 }
+  enum :status, { pending: 0, processing: 1, completed: 2, failed: 3 }, default: :pending
+
+  validates :captured_on, :source_url, :viewport, presence: true
+  validates :viewport, uniqueness: { scope: %i[newspaper_id captured_on] }
+
+  scope :recent, -> { order(captured_on: :desc, captured_at: :desc) }
 end
diff --git a/app/models/user.rb b/app/models/user.rb
index c88d5b0..2d68300 100644
--- a/app/models/user.rb
+++ b/app/models/user.rb
@@ -1,6 +1,14 @@
 class User < ApplicationRecord
   has_secure_password
   has_many :sessions, dependent: :destroy
+  has_many :activity_logs, dependent: :restrict_with_error
+
+  enum :role, { member: 0, admin: 1 }, default: :member
 
   normalizes :email_address, with: ->(e) { e.strip.downcase }
+
+  validates :name, presence: true
+  validates :email_address, presence: true, uniqueness: { case_sensitive: false }
+
+  scope :active, -> { where(active: true) }
 end
diff --git a/app/views/admin/newspapers/_form.html.erb b/app/views/admin/newspapers/_form.html.erb
new file mode 100644
index 0000000..839f1cb
--- /dev/null
+++ b/app/views/admin/newspapers/_form.html.erb
@@ -0,0 +1,6 @@
+<%= form_with model: [:admin, newspaper], class: "panel stack-form" do |form| %>
+  <% if newspaper.errors.any? %><div class="form-errors"><strong>Revise os campos:</strong><ul><% newspaper.errors.full_messages.each do |message| %><li><%= message %></li><% end %></ul></div><% end %>
+  <div class="form-grid"><div><%= form.label :name, "Nome" %><%= form.text_field :name, required: true %></div><div><%= form.label :slug, "Identificador" %><%= form.text_field :slug, placeholder: "gerado automaticamente" %></div><div class="full"><%= form.label :homepage_url, "URL da página" %><%= form.url_field :homepage_url, required: true, placeholder: "https://..." %></div><div><%= form.label :category, "Categoria" %><%= form.text_field :category, placeholder: "Nacional, regional..." %></div><div><%= form.label :country, "País" %><%= form.text_field :country %></div><div><%= form.label :time_zone, "Fuso horário" %><%= form.select :time_zone, ActiveSupport::TimeZone.all.map { |zone| [zone.to_s, zone.tzinfo.name] } %></div><div><%= form.label :capture_time, "Horário de captura" %><%= form.time_field :capture_time %></div></div>
+  <fieldset><legend>Formatos</legend><label class="check"><%= form.check_box :desktop_enabled %> Desktop</label><label class="check"><%= form.check_box :mobile_enabled %> Mobile</label><label class="check"><%= form.check_box :active %> Monitoramento ativo</label></fieldset>
+  <div class="form-actions"><%= link_to "Cancelar", admin_newspapers_path, class: "button secondary" %><%= form.submit "Salvar jornal", class: "button primary" %></div>
+<% end %>
diff --git a/app/views/admin/newspapers/edit.html.erb b/app/views/admin/newspapers/edit.html.erb
new file mode 100644
index 0000000..73bc7fa
--- /dev/null
+++ b/app/views/admin/newspapers/edit.html.erb
@@ -0,0 +1 @@
+<div class="page-heading"><div><p class="eyebrow">ADMINISTRAÇÃO</p><h1>Editar <%= @newspaper.name %></h1></div></div><%= render "form", newspaper: @newspaper %>
diff --git a/app/views/admin/newspapers/index.html.erb b/app/views/admin/newspapers/index.html.erb
new file mode 100644
index 0000000..eb465d4
--- /dev/null
+++ b/app/views/admin/newspapers/index.html.erb
@@ -0,0 +1,4 @@
+<div class="page-heading"><div><p class="eyebrow">ADMINISTRAÇÃO</p><h1>Jornais</h1><p class="muted">Gerencie as páginas monitoradas e seus formatos de captura.</p></div><%= link_to "Adicionar jornal", new_admin_newspaper_path, class: "button primary" %></div>
+<div class="table-card"><table><thead><tr><th>Jornal</th><th>Horário</th><th>Formatos</th><th>Status</th><th></th></tr></thead><tbody>
+<% @newspapers.each do |newspaper| %><tr><td><strong><%= newspaper.name %></strong><small><%= newspaper.homepage_url %></small></td><td><%= newspaper.capture_time.strftime("%H:%M") %></td><td><%= [newspaper.desktop_enabled? && "Desktop", newspaper.mobile_enabled? && "Mobile"].compact.join(" + ") %></td><td><span class="status <%= newspaper.active? ? "ok" : "off" %>"><%= newspaper.active? ? "Ativo" : "Pausado" %></span></td><td><%= link_to "Gerenciar", admin_newspaper_path(newspaper) %></td></tr><% end %>
+</tbody></table></div>
diff --git a/app/views/admin/newspapers/new.html.erb b/app/views/admin/newspapers/new.html.erb
new file mode 100644
index 0000000..49e421f
--- /dev/null
+++ b/app/views/admin/newspapers/new.html.erb
@@ -0,0 +1 @@
+<div class="page-heading"><div><p class="eyebrow">ADMINISTRAÇÃO</p><h1>Novo jornal</h1></div></div><%= render "form", newspaper: @newspaper %>
diff --git a/app/views/admin/newspapers/show.html.erb b/app/views/admin/newspapers/show.html.erb
new file mode 100644
index 0000000..5a67c89
--- /dev/null
+++ b/app/views/admin/newspapers/show.html.erb
@@ -0,0 +1,2 @@
+<div class="page-heading"><div><p class="eyebrow">JORNAL MONITORADO</p><h1><%= @newspaper.name %></h1><p class="muted"><%= @newspaper.homepage_url %></p></div><%= link_to "Editar", edit_admin_newspaper_path(@newspaper), class: "button primary" %></div>
+<div class="detail-grid panel"><div><span>Status</span><strong><%= @newspaper.active? ? "Ativo" : "Pausado" %></strong></div><div><span>Horário</span><strong><%= @newspaper.capture_time.strftime("%H:%M") %></strong></div><div><span>Fuso</span><strong><%= @newspaper.time_zone %></strong></div><div><span>Formatos</span><strong><%= [@newspaper.desktop_enabled? && "Desktop", @newspaper.mobile_enabled? && "Mobile"].compact.join(" + ") %></strong></div></div>
diff --git a/app/views/admin/users/index.html.erb b/app/views/admin/users/index.html.erb
new file mode 100644
index 0000000..30f737c
--- /dev/null
+++ b/app/views/admin/users/index.html.erb
@@ -0,0 +1,2 @@
+<div class="page-heading"><div><p class="eyebrow">ADMINISTRAÇÃO</p><h1>Usuários</h1><p class="muted">Controle quem pode consultar e administrar o acervo.</p></div><%= link_to "Convidar usuário", new_admin_user_path, class: "button primary" %></div>
+<div class="table-card"><table><thead><tr><th>Usuário</th><th>Perfil</th><th>Último acesso</th><th>Status</th><th></th></tr></thead><tbody><% @users.each do |user| %><tr><td><strong><%= user.name %></strong><small><%= user.email_address %></small></td><td><%= user.admin? ? "Administrador" : "Membro" %></td><td><%= user.last_sign_in_at ? l(user.last_sign_in_at, format: :short) : "Nunca" %></td><td><span class="status <%= user.active? ? "ok" : "off" %>"><%= user.active? ? "Ativo" : "Inativo" %></span></td><td><%= form_with model: [:admin, user], method: :patch, class: "inline-form" do |form| %><%= form.hidden_field :role, value: user.role %><%= form.hidden_field :active, value: !user.active? %><%= form.submit(user.active? ? "Desativar" : "Ativar", class: "text-action") %><% end %></td></tr><% end %></tbody></table></div>
diff --git a/app/views/admin/users/new.html.erb b/app/views/admin/users/new.html.erb
new file mode 100644
index 0000000..e46f708
--- /dev/null
+++ b/app/views/admin/users/new.html.erb
@@ -0,0 +1,2 @@
+<div class="page-heading"><div><p class="eyebrow">ADMINISTRAÇÃO</p><h1>Convidar usuário</h1><p class="muted">O usuário receberá um e-mail para definir a própria senha.</p></div></div>
+<%= form_with model: [:admin, @user], class: "panel stack-form narrow-panel" do |form| %><% if @user.errors.any? %><div class="form-errors"><%= @user.errors.full_messages.to_sentence %></div><% end %><%= form.label :name, "Nome" %><%= form.text_field :name, required: true %><%= form.label :email_address, "E-mail" %><%= form.email_field :email_address, required: true %><%= form.label :role, "Perfil" %><%= form.select :role, [["Membro", "member"], ["Administrador", "admin"]] %><%= form.hidden_field :active, value: true %><div class="form-actions"><%= link_to "Cancelar", admin_users_path, class: "button secondary" %><%= form.submit "Enviar convite", class: "button primary" %></div><% end %>
diff --git a/app/views/dashboard/index.html.erb b/app/views/dashboard/index.html.erb
index 8179f3e..bf1696b 100644
--- a/app/views/dashboard/index.html.erb
+++ b/app/views/dashboard/index.html.erb
@@ -1,2 +1,17 @@
-<h1>Dashboard#index</h1>
-<p>Find me in app/views/dashboard/index.html.erb</p>
+<div class="page-heading">
+  <div><p class="eyebrow">VISÃO GERAL</p><h1>Olá, <%= current_user.name.split.first %>.</h1><p class="muted">Acompanhe o acervo e a operação das capturas.</p></div>
+  <%= link_to "Abrir acervo", screenshots_path, class: "button primary" %>
+</div>
+<section class="stats-grid">
+  <article class="stat"><span>Jornais ativos</span><strong><%= @active_newspapers %></strong></article>
+  <article class="stat"><span>Capturas esperadas</span><strong><%= @capture_run&.expected_count || 0 %></strong></article>
+  <article class="stat success"><span>Concluídas hoje</span><strong><%= @capture_run&.completed_count || 0 %></strong></article>
+  <article class="stat danger"><span>Falhas hoje</span><strong><%= @capture_run&.failed_count || 0 %></strong></article>
+</section>
+<section class="section-block">
+  <div class="section-heading"><div><p class="eyebrow">MAIS RECENTES</p><h2>Últimas capturas</h2></div><%= link_to "Ver todas", screenshots_path %></div>
+  <div class="gallery-grid">
+    <% @recent_screenshots.each do |screenshot| %><%= link_to screenshot_path(screenshot), class: "capture-card" do %><div class="capture-image"><%= image_tag screenshot.image.variant(resize_to_limit: [640, 800]) if screenshot.image.attached? %></div><div class="capture-meta"><strong><%= screenshot.newspaper.name %></strong><span><%= l screenshot.captured_on %> · <%= screenshot.viewport.capitalize %></span></div><% end %><% end %>
+    <% if @recent_screenshots.empty? %><div class="empty-state"><strong>Nenhuma captura disponível</strong><p>As primeiras imagens aparecerão após a execução do bot.</p></div><% end %>
+  </div>
+</section>
diff --git a/app/views/layouts/application.html.erb b/app/views/layouts/application.html.erb
index 6c899a1..e86c295 100644
--- a/app/views/layouts/application.html.erb
+++ b/app/views/layouts/application.html.erb
@@ -24,6 +24,28 @@
   </head>
 
   <body>
-    <%= yield %>
+    <% if authenticated? %>
+      <header class="app-header">
+        <%= link_to root_path, class: "brand" do %>
+          <span class="brand-mark">NS</span>
+          <span><strong>Newspaper</strong><small>Screenshot Hub</small></span>
+        <% end %>
+        <nav>
+          <%= link_to "Acervo", screenshots_path %>
+          <% if current_user&.admin? %>
+            <%= link_to "Jornais", admin_newspapers_path %>
+            <%= link_to "Usuários", admin_users_path %>
+          <% end %>
+          <%= button_to "Sair", session_path, method: :delete, class: "link-button" %>
+        </nav>
+      </header>
+    <% end %>
+
+    <main class="page-shell <%= "auth-shell" unless authenticated? %>">
+      <% flash.each do |type, message| %>
+        <div class="flash <%= type %>"><%= message %></div>
+      <% end %>
+      <%= yield %>
+    </main>
   </body>
 </html>
diff --git a/app/views/screenshots/index.html.erb b/app/views/screenshots/index.html.erb
new file mode 100644
index 0000000..2e770ed
--- /dev/null
+++ b/app/views/screenshots/index.html.erb
@@ -0,0 +1,3 @@
+<div class="page-heading"><div><p class="eyebrow">ACERVO DIÁRIO</p><h1>Capturas de <%= l @date, format: :long %></h1></div></div>
+<%= form_with url: screenshots_path, method: :get, class: "filter-bar" do |form| %><%= form.date_field :date, value: @date %><%= form.collection_select :newspaper_id, @newspapers, :id, :name, { include_blank: "Todos os jornais", selected: params[:newspaper_id] } %><%= form.select :viewport, [["Todos os formatos", ""], ["Desktop", "desktop"], ["Mobile", "mobile"]], selected: params[:viewport] %><%= form.submit "Filtrar", class: "button primary" %><% end %>
+<div class="gallery-grid"><% @screenshots.each do |screenshot| %><%= link_to screenshot_path(screenshot), class: "capture-card" do %><div class="capture-image"><%= image_tag screenshot.image.variant(resize_to_limit: [640, 800]) if screenshot.image.attached? %></div><div class="capture-meta"><strong><%= screenshot.newspaper.name %></strong><span><%= screenshot.viewport.capitalize %> · <%= screenshot.captured_at&.strftime("%H:%M") %></span></div><% end %><% end %><% if @screenshots.empty? %><div class="empty-state"><strong>Nenhuma captura nesta data</strong><p>Escolha outro dia ou aguarde a próxima execução.</p></div><% end %></div>
diff --git a/app/views/screenshots/show.html.erb b/app/views/screenshots/show.html.erb
new file mode 100644
index 0000000..4c604f2
--- /dev/null
+++ b/app/views/screenshots/show.html.erb
@@ -0,0 +1 @@
+<div class="page-heading"><div><p class="eyebrow"><%= @screenshot.viewport.upcase %></p><h1><%= @screenshot.newspaper.name %></h1><p class="muted"><%= l @screenshot.captured_on, format: :long %> · <%= @screenshot.captured_at&.strftime("%H:%M") %></p></div><%= link_to "Voltar ao acervo", screenshots_path(date: @screenshot.captured_on), class: "button secondary" %></div><div class="screenshot-view"><%= image_tag @screenshot.image if @screenshot.image.attached? %></div>
diff --git a/app/views/sessions/new.html.erb b/app/views/sessions/new.html.erb
index ff641c4..1b01e9a 100644
--- a/app/views/sessions/new.html.erb
+++ b/app/views/sessions/new.html.erb
@@ -1,11 +1,12 @@
-<%= tag.div(flash[:alert], style: "color:red") if flash[:alert] %>
-<%= tag.div(flash[:notice], style: "color:green") if flash[:notice] %>
-
-<%= form_with url: session_path do |form| %>
-  <%= form.email_field :email_address, required: true, autofocus: true, autocomplete: "username", placeholder: "Enter your email address", value: params[:email_address] %><br>
-  <%= form.password_field :password, required: true, autocomplete: "current-password", placeholder: "Enter your password", maxlength: 72 %><br>
-  <%= form.submit "Sign in" %>
-<% end %>
-<br>
-
-<%= link_to "Forgot password?", new_password_path %>
+<section class="auth-card">
+  <div class="brand auth-brand"><span class="brand-mark">NS</span><span><strong>Newspaper</strong><small>Screenshot Hub</small></span></div>
+  <p class="eyebrow">ACESSO RESTRITO</p>
+  <h1>Entre no acervo</h1>
+  <p class="muted">Consulte diariamente as páginas capturadas dos jornais monitorados.</p>
+  <%= form_with url: session_path, class: "stack-form" do |form| %>
+    <label>E-mail</label><%= form.email_field :email_address, required: true, autofocus: true, autocomplete: "username", placeholder: "voce@empresa.com.br", value: params[:email_address] %>
+    <label>Senha</label><%= form.password_field :password, required: true, autocomplete: "current-password", placeholder: "Sua senha", maxlength: 72 %>
+    <%= form.submit "Entrar", class: "button primary" %>
+  <% end %>
+  <%= link_to "Esqueci minha senha", new_password_path, class: "auth-link" %>
+</section>
diff --git a/config/application.rb b/config/application.rb
index 0d0f4f4..89220ee 100644
--- a/config/application.rb
+++ b/config/application.rb
@@ -23,6 +23,9 @@ module NewspaperScreenshotHub
     # Initialize configuration defaults for originally generated Rails version.
     config.load_defaults 8.1
 
+    config.i18n.default_locale = :"pt-BR"
+    config.time_zone = "America/Sao_Paulo"
+
     # Please, add to the `ignore` list any other `lib` subdirectories that do
     # not contain `.rb` files, or that should not be reloaded or eager loaded.
     # Common ones are `templates`, `generators`, or `middleware`, for example.
diff --git a/config/locales/pt-BR.yml b/config/locales/pt-BR.yml
new file mode 100644
index 0000000..9311fd2
--- /dev/null
+++ b/config/locales/pt-BR.yml
@@ -0,0 +1,29 @@
+pt-BR:
+  date:
+    formats:
+      default: "%d/%m/%Y"
+      short: "%d/%m"
+      long: "%d de %B de %Y"
+    month_names: [~, janeiro, fevereiro, março, abril, maio, junho, julho, agosto, setembro, outubro, novembro, dezembro]
+  time:
+    formats:
+      default: "%d/%m/%Y, %H:%M"
+      short: "%d/%m/%Y, %H:%M"
+  activerecord:
+    models:
+      newspaper: Jornal
+      user: Usuário
+    attributes:
+      newspaper:
+        name: Nome
+        homepage_url: URL
+        time_zone: Fuso horário
+        capture_time: Horário de captura
+      user:
+        name: Nome
+        email_address: E-mail
+  errors:
+    messages:
+      blank: não pode ficar em branco
+      taken: já está em uso
+      invalid: não é válido
diff --git a/config/routes.rb b/config/routes.rb
index a559554..c45e0c2 100644
--- a/config/routes.rb
+++ b/config/routes.rb
@@ -1,8 +1,13 @@
 Rails.application.routes.draw do
-  get "dashboard/index"
   resource :session
   root "dashboard#index"
   resources :passwords, param: :token
+  resources :screenshots, only: %i[index show]
+
+  namespace :admin do
+    resources :newspapers
+    resources :users, only: %i[index new create update]
+  end
   # Define your application routes per the DSL in https://guides.rubyonrails.org/routing.html
 
   # Reveal health status on /up that returns 200 if the app boots with no exceptions, otherwise 500.
diff --git a/db/migrate/20260826210000_harden_mvp_schema.rb b/db/migrate/20260826210000_harden_mvp_schema.rb
new file mode 100644
index 0000000..724e304
--- /dev/null
+++ b/db/migrate/20260826210000_harden_mvp_schema.rb
@@ -0,0 +1,30 @@
+class HardenMvpSchema < ActiveRecord::Migration[8.1]
+  def change
+    change_column_default :users, :role, from: nil, to: 0
+    change_column_default :users, :active, from: nil, to: true
+
+    change_column_default :newspapers, :country, from: nil, to: "Brasil"
+    change_column_default :newspapers, :time_zone, from: nil, to: "America/Sao_Paulo"
+    change_column_default :newspapers, :desktop_enabled, from: nil, to: true
+    change_column_default :newspapers, :mobile_enabled, from: nil, to: true
+    change_column_default :newspapers, :active, from: nil, to: true
+    change_column_default :newspapers, :capture_options, from: nil, to: {}
+
+    change_column_default :capture_runs, :status, from: nil, to: 0
+    change_column_default :capture_runs, :expected_count, from: nil, to: 0
+    change_column_default :capture_runs, :completed_count, from: nil, to: 0
+    change_column_default :capture_runs, :failed_count, from: nil, to: 0
+
+    change_column_default :screenshots, :status, from: nil, to: 0
+    change_column_default :screenshots, :attempts, from: nil, to: 0
+    change_column_default :screenshots, :metadata, from: nil, to: {}
+    change_column_default :activity_logs, :metadata, from: nil, to: {}
+
+    add_index :newspapers, :slug, unique: true
+    add_index :newspapers, :active
+    add_index :capture_runs, :scheduled_for, unique: true
+    add_index :screenshots, %i[newspaper_id captured_on viewport], unique: true, name: "idx_screenshots_daily_viewport"
+    add_index :screenshots, %i[captured_on status]
+    add_index :activity_logs, %i[auditable_type auditable_id]
+  end
+end
diff --git a/db/seeds.rb b/db/seeds.rb
index 4fbd6ed..dafe08f 100644
--- a/db/seeds.rb
+++ b/db/seeds.rb
@@ -2,8 +2,9 @@
 # development, test). The code here should be idempotent so that it can be executed at any point in every environment.
 # The data can then be loaded with the bin/rails db:seed command (or created alongside the database with db:setup).
 #
-# Example:
-#
-#   ["Action", "Comedy", "Drama", "Horror"].each do |genre_name|
-#     MovieGenre.find_or_create_by!(name: genre_name)
-#   end
+admin_email = ENV.fetch("ADMIN_EMAIL", "admin@example.com")
+
+unless User.exists?(email_address: admin_email)
+  admin_password = Rails.env.production? ? ENV.fetch("ADMIN_PASSWORD") : ENV.fetch("ADMIN_PASSWORD", "change-me-in-development")
+  User.create!(name: ENV.fetch("ADMIN_NAME", "Administrador"), email_address: admin_email, role: :admin, active: true, password: admin_password, password_confirmation: admin_password)
+end
diff --git a/test/controllers/admin/newspapers_controller_test.rb b/test/controllers/admin/newspapers_controller_test.rb
new file mode 100644
index 0000000..62e31a6
--- /dev/null
+++ b/test/controllers/admin/newspapers_controller_test.rb
@@ -0,0 +1,15 @@
+require "test_helper"
+
+class Admin::NewspapersControllerTest < ActionDispatch::IntegrationTest
+  test "member cannot access admin area" do
+    sign_in_as users(:two)
+    get admin_newspapers_url
+    assert_redirected_to root_url
+  end
+
+  test "admin can access newspapers" do
+    sign_in_as users(:one)
+    get admin_newspapers_url
+    assert_response :success
+  end
+end
diff --git a/test/controllers/dashboard_controller_test.rb b/test/controllers/dashboard_controller_test.rb
index e5b5cb7..dd936a7 100644
--- a/test/controllers/dashboard_controller_test.rb
+++ b/test/controllers/dashboard_controller_test.rb
@@ -2,7 +2,13 @@ require "test_helper"
 
 class DashboardControllerTest < ActionDispatch::IntegrationTest
   test "should get index" do
-    get dashboard_index_url
+    get root_url
     assert_redirected_to new_session_url
   end
+
+  test "authenticated user gets dashboard" do
+    sign_in_as users(:one)
+    get root_url
+    assert_response :success
+  end
 end
diff --git a/test/controllers/passwords_controller_test.rb b/test/controllers/passwords_controller_test.rb
index e1a1b03..dc700c1 100644
--- a/test/controllers/passwords_controller_test.rb
+++ b/test/controllers/passwords_controller_test.rb
@@ -46,7 +46,7 @@ class PasswordsControllerTest < ActionDispatch::IntegrationTest
     end
 
     follow_redirect!
-    assert_notice "Password has been reset"
+    assert_notice "Senha definida com sucesso"
   end
 
   test "update with non matching passwords" do
@@ -57,7 +57,7 @@ class PasswordsControllerTest < ActionDispatch::IntegrationTest
     end
 
     follow_redirect!
-    assert_notice "Passwords did not match"
+    assert_notice "As senhas não coincidem"
   end
 
   private
diff --git a/test/fixtures/capture_runs.yml b/test/fixtures/capture_runs.yml
index 9c87afa..748ddc2 100644
--- a/test/fixtures/capture_runs.yml
+++ b/test/fixtures/capture_runs.yml
@@ -11,7 +11,7 @@ one:
   error_summary: MyText
 
 two:
-  scheduled_for: 2026-08-26
+  scheduled_for: 2026-08-25
   started_at: 2026-08-26 20:07:12
   finished_at: 2026-08-26 20:07:12
   status: 1
diff --git a/test/fixtures/newspapers.yml b/test/fixtures/newspapers.yml
index 3a8d1c5..1b1372d 100644
--- a/test/fixtures/newspapers.yml
+++ b/test/fixtures/newspapers.yml
@@ -1,27 +1,27 @@
 # Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html
 
 one:
-  name: MyString
-  slug: MyString
-  homepage_url: MyString
-  category: MyString
-  country: MyString
-  time_zone: MyString
+  name: Jornal Um
+  slug: jornal-um
+  homepage_url: https://example.com/jornal-um
+  category: Nacional
+  country: Brasil
+  time_zone: America/Sao_Paulo
   capture_time: 2026-08-26 20:07:11
-  desktop_enabled: false
-  mobile_enabled: false
-  active: false
-  capture_options: 
+  desktop_enabled: true
+  mobile_enabled: true
+  active: true
+  capture_options: {}
 
 two:
-  name: MyString
-  slug: MyString
-  homepage_url: MyString
-  category: MyString
-  country: MyString
-  time_zone: MyString
+  name: Jornal Dois
+  slug: jornal-dois
+  homepage_url: https://example.com/jornal-dois
+  category: Regional
+  country: Brasil
+  time_zone: America/Sao_Paulo
   capture_time: 2026-08-26 20:07:11
-  desktop_enabled: false
+  desktop_enabled: true
   mobile_enabled: false
-  active: false
-  capture_options: 
+  active: true
+  capture_options: {}
diff --git a/test/fixtures/users.yml b/test/fixtures/users.yml
index 0951563..b516156 100644
--- a/test/fixtures/users.yml
+++ b/test/fixtures/users.yml
@@ -1,9 +1,15 @@
 <% password_digest = BCrypt::Password.create("password") %>
 
 one:
+  name: Usuário Um
   email_address: one@example.com
   password_digest: <%= password_digest %>
+  role: 1
+  active: true
 
 two:
+  name: Usuário Dois
   email_address: two@example.com
   password_digest: <%= password_digest %>
+  role: 0
+  active: true
diff --git a/test/models/newspaper_test.rb b/test/models/newspaper_test.rb
index 968134a..0788965 100644
--- a/test/models/newspaper_test.rb
+++ b/test/models/newspaper_test.rb
@@ -1,7 +1,16 @@
 require "test_helper"
 
 class NewspaperTest < ActiveSupport::TestCase
-  # test "the truth" do
-  #   assert true
-  # end
+  test "generates slug from name" do
+    newspaper = Newspaper.new(name: "Correio da Manhã", homepage_url: "https://example.com", time_zone: "America/Sao_Paulo", capture_time: "08:00", desktop_enabled: true)
+    newspaper.validate
+    assert_equal "correio-da-manha", newspaper.slug
+  end
+
+  test "requires at least one viewport" do
+    newspaper = newspapers(:one)
+    newspaper.desktop_enabled = false
+    newspaper.mobile_enabled = false
+    assert_not newspaper.valid?
+  end
 end
diff --git a/test/models/user_test.rb b/test/models/user_test.rb
index 83445c4..0bf183c 100644
--- a/test/models/user_test.rb
+++ b/test/models/user_test.rb
@@ -5,4 +5,8 @@ class UserTest < ActiveSupport::TestCase
     user = User.new(email_address: " DOWNCASED@EXAMPLE.COM ")
     assert_equal("downcased@example.com", user.email_address)
   end
+
+  test "defaults to member" do
+    assert User.new.member?
+  end
 end
